# A few initial definitions just to make sure all required packages are installed. Change as needed.
# NOTE: It may echo some weird messages to the PDF on the first compile (package installation messages). Run twice and the problem will (hopefully) go away.
if (!require(ggplot2, quietly = TRUE)){
install.packages("ggplot2")
}
if (!require(devtools, quietly = TRUE)){
install.packages("devtools")
}
if (!require(broom, quietly = TRUE)){
devtools::install_github("dgrtwo/broom")
}
if (!require(ExpDE, quietly = TRUE)){
install.packages("ExpDE")
}
if (!require(cowplot, quietly = TRUE, warn.conflicts = FALSE)){
install.packages("cowplot")
}
if (!require(asbio, quietly = TRUE, warn.conflicts = FALSE)){
install.packages("asbio")
}
if (!require(car, quietly = TRUE, warn.conflicts = FALSE)){
install.packages("car")
}
dados_20162 <- read.csv(file="20162.csv",head=TRUE,sep=",")
dados_20171 <- read.csv(file="201171.csv",head=TRUE,sep=",")
dados_20162 <- read.csv(file="20162.csv",head=TRUE,sep=",")
dados_20171 <- read.csv(file="20171.csv",head=TRUE,sep=",")
dados_20162
dados_20162$Course[49]
dados_20162$Gender[49]
dados_20162$Height[49]
help length()
# A few initial definitions just to make sure all required packages are installed. Change as needed.
# NOTE: It may echo some weird messages to the PDF on the first compile (package installation messages). Run twice and the problem will (hopefully) go away.
if (!require(ggplot2, quietly = TRUE)){
install.packages("ggplot2")
}
if (!require(devtools, quietly = TRUE)){
install.packages("devtools")
}
if (!require(broom, quietly = TRUE)){
devtools::install_github("dgrtwo/broom")
}
if (!require(ExpDE, quietly = TRUE)){
install.packages("ExpDE")
}
if (!require(cowplot, quietly = TRUE, warn.conflicts = FALSE)){
install.packages("cowplot")
}
if (!require(asbio, quietly = TRUE, warn.conflicts = FALSE)){
install.packages("asbio")
}
if (!require(car, quietly = TRUE, warn.conflicts = FALSE)){
install.packages("car")
}
if (!require(EnvStats, quietly = TRUE, warn.conflicts = FALSE)){
install.packages("EnvStats")
}
rm(list = ls())
x <- 1
while ( x <= length(dados_20162))
{
if (dados_20162$Course=="PPGEE")
{
if(dados_20162$Gender=="M")   { m_162$H[x] = dados_20162$Height[x]    } else { f_162$H[x] = dados_20162$Height[x] }
}
x=x+1
}
# A few initial definitions just to make sure all required packages are installed. Change as needed.
# NOTE: It may echo some weird messages to the PDF on the first compile (package installation messages). Run twice and the problem will (hopefully) go away.
##if (!require(ggplot2, quietly = TRUE)){
##    install.packages("ggplot2")
##  }
##if (!require(devtools, quietly = TRUE)){
##      install.packages("devtools")
##      }
## if (!require(broom, quietly = TRUE)){
##       devtools::install_github("dgrtwo/broom")
##      }
##if (!require(ExpDE, quietly = TRUE)){
##        install.packages("ExpDE")
##      }
##if (!require(cowplot, quietly = TRUE, warn.conflicts = FALSE)){
##        install.packages("cowplot")
##}
##if (!require(asbio, quietly = TRUE, warn.conflicts = FALSE)){
##        install.packages("asbio")
##}
##if (!require(car, quietly = TRUE, warn.conflicts = FALSE)){
##        install.packages("car")
##}
##if (!require(EnvStats, quietly = TRUE, warn.conflicts = FALSE)){
##        install.packages("EnvStats")
##}
##rm(list = ls())
dados_20162 <- read.csv(file="20162.csv",head=TRUE,sep=",")
dados_20171 <- read.csv(file="20171.csv",head=TRUE,sep=",")
x <- 1
while ( x <= length(dados_20162))
{
if (dados_20162$Course=="PPGEE")
{
if(dados_20162$Gender=="M")   { m_162$H[x] = dados_20162$Height[x]    } else { f_162$H[x] = dados_20162$Height[x] }
}
x=x+1
}
dados_20171 <- read.csv(file="20171.csv",head=TRUE,sep=",")
